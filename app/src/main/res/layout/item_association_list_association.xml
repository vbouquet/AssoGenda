<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/item_association_list_association.logo"
        android:layout_width="64dp"
        android:layout_height="66dp"
        app:srcCompat="@drawable/association_default_icon" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:weightSum="1">

        <TextView
            android:text="Nom"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:id="@+id/item_association_list_association.name" />

        <TextView
            android:text="Université"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:id="@+id/item_association_list_association.university" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/item_association_list_association.follow"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        app:srcCompat="@android:drawable/btn_star_big_off" />

</LinearLayout>